#include<stdio.h>
#include<stdlib.h>

int main() {
	int n = 10;
	// general syntax
	// malloc(sizeof(<datatype> * size_of_array);
	// calloc(size_of_array, sizeof(<datatype>); - slow - initialize to 0;
}
